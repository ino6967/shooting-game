# CLAUDE.md

## 文章スタイル
- コメント・コミットメッセージ・返信など、人間が読む文章は最小限の単語数にする。要点だけ書く。
- 誇張表現・称賛語(「素晴らしい実装です」等)を避ける。
- 「〜だと思います」等の曖昧な前置きを避け、結論から書く。

## コード変更の原則
- 実装する機能に関係ないコードブロックには触れない。自分が作成/修正していないコードにコメントを追加しない。
- 変更行数は可能な限り最小限にする。差分を最小化し、レビューしやすくする。
- 既存のコードスタイル・命名規則に合わせる。勝手にリファクタリングしない。

## レイヤー分離
- 低レベル処理(GASのSpreadsheetApp直叩き、Chrome拡張のchrome.* API呼び出し、外部API通信など)は専用のドライバ/ラッパー関数に隔離する。
- 上位のロジック(業務処理・UI)は、ラッパー経由でドメイン概念に基づいたAPIだけを呼ぶ。生のAPI呼び出しを直接埋め込まない。
- レイヤーの境界を飛び越えない。例:UIコンポーネントが直接スプレッドシートやDBを叩かない。必ず中間層を経由する。

## プロジェクト共通の注意点
- HTML/Webテンプレート・SEO関連のコードで `<meta name="robots" content="noindex,nofollow">` を含めない。デフォルトはインデックス可能な状態にする。
- 一人開発が前提。過剰な抽象化・将来の拡張性を見越した設計より、今動くシンプルな実装を優先する。
- GAS特有の制約(実行時間制限、トリガーの同時実行制限など)に注意し、長時間処理は分割を検討する。

## 運用
- 同じ指摘を繰り返すようになったら、このファイルにルールを追記する。エディタを開かず「このルールをCLAUDE.mdに追加して」と頼めばよい。
- 新規プロジェクトのルート直下に配置する。
